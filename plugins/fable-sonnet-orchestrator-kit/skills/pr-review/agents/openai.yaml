interface:
  display_name: "PR Review"
  short_description: "Verify one executor PR against its issue"
  default_prompt: "Use $pr-review to verify this executor pull request."
policy:
  allow_implicit_invocation: false
